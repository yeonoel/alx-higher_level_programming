-- Lists all records of the table second_table
-- Results should display both the score and the name
-- Score should be ordered by score (top first)
SELECT score, name
FROM `second_table`
ORDER BY `score` DESC;
